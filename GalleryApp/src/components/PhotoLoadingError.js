import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

export const PhotoLoadingError = (props) => {
  return (
    <View>
      <Text>PhotoLoadingError</Text>
    </View>
  )
}

const styles = StyleSheet.create({})